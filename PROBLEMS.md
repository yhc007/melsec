# 발견된 문제점 및 개선사항

## 1. 터미널 환경 문제
- 현재 환경: `TERM=dumb`, `not a tty`
- 실제 터미널에서 실행해야 TUI가 동작합니다
- SSH나 일반 터미널에서 실행 시 정상 동작할 것입니다

## 2. 코드 개선 필요 사항

### 2.1. 에러 처리 및 정리 코드
- panic 발생 시 터미널이 복구되지 않을 수 있음
- `Drop` 트레이트를 사용한 자동 정리 추가 필요

### 2.2. 포트 편집 로직 문제
- 현재 포트 편집 로직이 숫자를 추가만 하고 수정이 어려움
- 전체 포트 번호를 입력받도록 개선 필요

### 2.3. Tokio 런타임 관리
- 백그라운드 스레드에서 런타임을 실행하는 방식이 비효율적
- 런타임이 실제로 필요한 작업을 처리하지 못할 수 있음

### 2.4. 입력 모드 표시
- 현재 어떤 필드를 편집 중인지 UI에 표시되지 않음
- 편집 중임을 나타내는 표시 추가 필요

### 2.5. 네트워크/PC 편집 모드 없음
- F1, F2, F3, F4, F5만 있고 네트워크와 PC 편집 모드가 빠짐
- InputMode enum에는 정의되어 있지만 실제 키 처리 없음

