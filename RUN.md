# 실행 가이드

## GUI 프로그램 실행 방법

### 1. 빌드 (이미 완료된 경우 생략 가능)
```bash
cargo build --release
```

### 2. 실행

#### 방법 1: Xvfb 스크립트 사용 (권장 - OpenGL 오류 시)
```bash
./run_gui.sh
```
이 방법은 가상 디스플레이를 생성하고 소프트웨어 렌더링을 자동으로 설정합니다.

#### 방법 2: 직접 실행
터미널에서 다음 명령 중 하나를 실행하세요:

```bash
# 방법 1: 빌드된 실행 파일 직접 실행
./target/release/melsec-plc

# 방법 2: cargo로 실행 (개발 모드)
cargo run

# 방법 3: cargo로 릴리즈 모드 실행
cargo run --release
```

**참고**: 코드에 소프트웨어 렌더링이 이미 설정되어 있어 환경 변수 없이도 실행 가능합니다.
만약 OpenGL 오류가 발생한다면 다음과 같이 실행하세요:

```bash
LIBGL_ALWAYS_SOFTWARE=1 ./target/release/melsec-plc
```

### 3. 사용 방법

1. **PLC 연결**
   - IP 주소 입력 (기본값: 192.168.21.112)
   - 포트 번호 확인 (기본값: 5007)
   - 네트워크 번호와 PC 번호 설정
   - "연결" 버튼 클릭

2. **데이터 읽기 설정**
   - 디바이스 타입 선택 (예: D, M, X, Y)
   - 시작 주소 입력 (예: 0)
   - 읽을 개수 입력 (예: 10)
   - 비트 디바이스인 경우 체크박스 선택

3. **데이터 읽기**
   - "읽기" 버튼 클릭: 한 번만 읽기
   - "자동 읽기" 체크: 지정된 간격으로 자동 읽기

4. **데이터 확인**
   - 읽은 데이터가 표로 표시됩니다
   - 워드 디바이스: 10진수와 16진수 표시
   - 비트 디바이스: ON/OFF 상태 표시

### 문제 해결

- **GUI 창이 열리지 않는 경우**
  - DISPLAY 환경 변수 확인: `echo $DISPLAY`
  - X11 또는 Wayland 디스플레이 서버가 실행 중인지 확인
  - 소프트웨어 렌더링 사용: `LIBGL_ALWAYS_SOFTWARE=1 ./target/release/melsec-plc`

- **OpenGL 오류가 발생하는 경우**
  - 프로그램 내부에서 자동으로 소프트웨어 렌더링을 사용하도록 설정되어 있습니다
  - 여전히 오류가 발생하면: `LIBGL_ALWAYS_SOFTWARE=1 cargo run --release`

- **연결 실패**
  - PLC IP 주소가 올바른지 확인
  - 네트워크 연결 확인: `ping <PLC_IP>`
  - PLC에서 MC Protocol 통신이 활성화되어 있는지 확인

- **데이터를 읽지 못하는 경우**
  - 디바이스 타입이 올바른지 확인 (대소문자 구분)
  - 주소 범위가 유효한지 확인
  - PLC 연결 상태 확인
