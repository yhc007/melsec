# 🔍 PLC 통신 문제 진단 및 해결

## ❌ 발생한 문제

**증상**: PLC 데이터 읽기 시 타임아웃 발생
```
D7000 읽기 오류: 타임아웃
```

## 🔎 원인 분석

### 1단계: 네트워크 진단
```bash
./diagnose_plc.sh
```

**결과**:
- ✅ PING 성공 - 네트워크 연결 정상
- ✅ 포트 5010 열림 - TCP 연결 가능
- ❌ 데이터 읽기 타임아웃

### 2단계: 패킷 분석
```
→ SEND (21 bytes): 50 00 00 FF FF 03 00 0A 00 00 00 01 04 00 00 A8 58 1B 00 01 00
← RECV (15 bytes): D0 00 00 FF FF 03 00 0B 00 56 C0 00 FF FF 03
                                                  ^^^^^ 
                                          에러 코드: 0xC056
```

### 3단계: 에러 코드 해석

**에러 코드 0xC056**: **"지정된 디바이스가 범위를 벗어났거나 잘못된 주소입니다"**

## 🎯 문제의 핵심

**D7000 주소가 PLC에서 사용할 수 없는 주소**입니다!

- PLC 모델에 따라 사용 가능한 D 디바이스 범위가 다름
- D7000은 일부 PLC 모델에서 범위를 초과함
- 일반적으로 D0~D999, D0~D7999 등의 범위 사용

## ✅ 해결 방법

### 1. 주소 범위 확인

PLC 매뉴얼에서 D 디바이스의 사용 가능 범위를 확인하세요.

**일반적인 범위**:
- FX 시리즈: D0~D7999
- Q 시리즈: D0~D11999
- iQ-R 시리즈: D0~D32767

### 2. 주소 변경

현재 설정된 주소를 PLC에서 지원하는 범위로 변경하세요.

**현재 설정** (문제 있음):
```
D7000, D7002, D7004, D7006, D7008, D7010, D7012, D7014, D7016
```

**권장 설정** (안전한 범위):
```
D0, D2, D4, D6, D8, D10, D12, D14, D16
또는
D100, D102, D104, D106, D108, D110, D112, D114, D116
또는
D1000, D1002, D1004, D1006, D1008, D1010, D1012, D1014, D1016
```

### 3. 주소 변경 방법

**방법 A: 소스 코드 수정**
```bash
# src/main_tui.rs 파일 수정
vim src/main_tui.rs
```

118~127번째 라인의 주소를 변경:
```rust
addresses: vec![
    "D100".to_string(),   // D7000 → D100
    "D102".to_string(),   // D7002 → D102
    "D104".to_string(),   // D7004 → D104
    "D106".to_string(),   // D7006 → D106
    "D108".to_string(),   // D7008 → D108
    "D110".to_string(),   // D7010 → D110
    "D112".to_string(),   // D7012 → D112
    "D114".to_string(),   // D7014 → D114
    "D116".to_string(),   // D7016 → D116
],
```

그 후 다시 빌드:
```bash
cargo build --release
```

**방법 B: TUI에서 직접 수정** (프로그램 실행 중)
1. 프로그램 실행
2. **E** 키 - 주소 편집 모드
3. 사용 가능한 주소 입력 (예: D100, D102, ...)
4. **Enter** - 저장

## 🔧 적용된 개선 사항

### 1. 타임아웃 증가
```rust
// 3초 → 10초로 증가
client.set_timeout(Duration::from_secs(10));
```

### 2. 에러 메시지 개선
이제 명확한 에러 메시지가 표시됩니다:
```
PLC 응답 오류: 코드 0xC056 - 지정된 디바이스가 범위를 벗어났거나 잘못된 주소입니다
```

### 3. 에러 코드 목록
| 코드 | 의미 |
|------|------|
| 0xC050 | 데이터 형식 오류 |
| 0xC051 | 데이터 길이 오류 |
| 0xC056 | 디바이스 범위 초과 또는 잘못된 주소 |
| 0xC059 | 지정된 디바이스가 존재하지 않음 |
| 0xC05B | 읽기/쓰기 요청 길이 오류 |
| 0xC05C | ASCII 변환 오류 |
| 0xC05F | 프레임 구조 오류 |
| 0xC060 | 프레임 길이 오류 |
| 0xC061 | 잘못된 CPU 모델 |

## 📝 테스트 절차

### 1. 진단 실행
```bash
cd /home/root1/Work/RS_PLC
./diagnose_plc.sh
```

### 2. 주소 변경 후 테스트
```bash
# 주소를 D100~D116으로 변경했다고 가정
./target/release/melsec-plc-tui
```

### 3. 프로그램 사용
1. **C** 키 - PLC 연결
2. **R** 키 - 데이터 읽기
3. 에러 메시지 확인
   - 여전히 0xC056 에러가 나온다면 더 낮은 주소 사용
   - 성공하면 데이터가 테이블에 표시됨

### 4. 로그 확인
```bash
# 실시간 로그
tail -f tui_debug.log

# 성공 메시지 확인
grep "PLC 데이터 읽기 성공" tui_debug.log
```

## 💡 권장 사항

1. **PLC 매뉴얼 확인**
   - 사용 중인 PLC 모델의 D 디바이스 범위 확인
   - 프로그램에서 사용할 주소가 범위 내에 있는지 확인

2. **단계적 테스트**
   - D0부터 시작해서 테스트
   - 성공하면 필요한 주소로 변경

3. **에러 로그 확인**
   - 에러가 발생하면 에러 코드를 확인
   - 에러 코드에 따라 적절한 조치

## 📂 관련 파일

- `/home/root1/Work/RS_PLC/src/main_tui.rs` - 주소 설정 (118~127번 라인)
- `/home/root1/Work/RS_PLC/diagnose_plc.sh` - 진단 도구
- `/home/root1/Work/RS_PLC/tui_debug.log` - 디버그 로그
- `/home/root1/Work/RS_PLC/packet_debug.log` - 패킷 로그

## 🎉 최종 확인

주소를 올바른 범위로 변경한 후:
```bash
./target/release/melsec-plc-tui
```

프로그램 실행 시 다음과 같이 표시되어야 합니다:
```
┌─────────────────────────────────────┐
│ 데이터 (9개)                        │
├──────────┬──────────┬──────────────┤
│ 주소     │ 값(10진) │ 값(16진)     │
├──────────┼──────────┼──────────────┤
│ D100     │ 1234     │ 0x04D2       │
│ D102     │ 5678     │ 0x162E       │
│ ...      │ ...      │ ...          │
└──────────┴──────────┴──────────────┘
```
